// 1 Crea una función que reciba un array de números y devuelva un nuevo array con cada número duplicado usando el método .map
const numeros = [2, 3, 4, 5, 6, 6]

console.log(numeros.map((numero) => numero * 2))