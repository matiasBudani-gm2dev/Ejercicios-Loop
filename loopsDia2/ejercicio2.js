// 2 Escribe una función que reciba un array de números y devuelva un nuevo array con cada número convertido a string usando el método .map.
const array = [2, 5, 5, 1, 2, 4, 21 , 123]

console.log(array.map((numero) => numero.toString()))